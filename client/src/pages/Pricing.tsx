/** ToolImage pricing — Monochrome Instrument: transparent feature framing without a simulated checkout. */
/** ToolImage pricing — Monochrome Instrument: clear availability language, no fake checkout, and honest future value. */
import { Check, LockKeyhole } from "lucide-react";
import { motion, useReducedMotion } from "framer-motion";
import { useState } from "react";
import { Link } from "wouter";
import { SiteShell } from "@/components/SiteShell";
import { Seo } from "@/components/Seo";
import { PricingRegionSelector } from "@/components/PricingRegionSelector";
import { formatRegionalPrice, getInitialPricingRegion, PricingRegionId, REGIONAL_PRICES, setPricingRegionForSession } from "@/lib/pricing";

const freeItems = ["Single-image and target-size compression", "Single-image resizing with manual pixels", "Single-image JPG, PNG, and WebP conversion", "Standard presets and client-side processing"];
const proItems = ["Batch processing", "Custom saved presets", "Download batches as ZIP"];

export default function Pricing() { const [region, setRegion] = useState<PricingRegionId>(() => getInitialPricingRegion()); const reduceMotion = useReducedMotion(); const price = formatRegionalPrice(region); const chooseRegion = (next: PricingRegionId) => { setRegion(next); setPricingRegionForSession(next); }; return <SiteShell><Seo title="ToolImage Pricing — Free Local Image Tools" description="ToolImage’s core image compression, resize, and conversion tools are free and run locally in your browser." path="/pricing" noIndex /><main id="main-content"><section className="pricing-hero pricing-hero--regional"><div className="pricing-rail"><span>01</span><i /><span>CAPABILITY INDEX</span><small>NO PAYMENT FLOW ACTIVE</small></div><p className="eyebrow">SIMPLE BY DESIGN</p><h1>Useful now.<br /><em>More when it matters.</em></h1><p>Every core tool is free, works locally in your browser, and does not require an account.</p><div className="pricing-region-row"><span>Regional display</span><PricingRegionSelector region={region} onChange={chooseRegion} /></div></section><section className="pricing-cards"><motion.article className="pricing-card pricing-card--free" initial={{ opacity: 0, y: reduceMotion ? 0 : 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: reduceMotion ? 0 : 0.28 }}><p className="eyebrow">AVAILABLE NOW</p><h2>Free</h2><p>For everyday image tasks.</p><div className="pricing-card__price"><strong>Free</strong><span>No account required</span></div><ul>{freeItems.map((item) => <li key={item}><Check size={15} />{item}</li>)}</ul><Link href="/compress-image" className="primary-button">Start using ToolImage</Link></motion.article><motion.article className="pricing-card pricing-card--pro" initial={{ opacity: 0, y: reduceMotion ? 0 : 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: reduceMotion ? 0 : 0.28, delay: reduceMotion ? 0 : 0.06 }}><p className="eyebrow">TOOLIMAGE PRO</p><h2>Process more.<br /><em>Do less.</em></h2><p>For a future local workflow built around less repetition.</p><div className="pricing-card__price"><strong>{price}<small>/ month</small></strong><span>{REGIONAL_PRICES[region].country} display price</span></div><ul>{proItems.map((item) => <li key={item}><Check size={15} />{item}</li>)}</ul><button type="button" className="secondary-button" disabled><LockKeyhole size={15} /> Coming soon</button><small className="payment-note">Payment integration is prepared, not active. This display uses browser locale or your session selection; no IP is collected.</small></motion.article></section><section className="pricing-note"><span>NO SURPRISES</span><i /><p>ToolImage will never suggest a payment is available before a real, secure purchase flow exists.</p></section></main></SiteShell>; }
